import CardList from "../Layout/CardList";

const Rice = () => {
  return (
    <section className="ml-28 md:ml-32 mt-10">
      <CardList typeFilter="rice" />
    </section>
  );
};

export default Rice;
